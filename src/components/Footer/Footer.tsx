import React from "react";

const Footer: React.FC = () => {
  return (
    <footer className="footer">
      <div>© Supersales LLC All Rights Reserved</div>
      {/* <div>{version !== null && version !== "" && version?.length > 0 && `Version ${version}`}</div> */}
    </footer>
  );
};
export default Footer;
